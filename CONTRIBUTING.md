# 贡献指南

感谢你对 moi-hardware-inquiry 的关注！本文件说明如何贡献代码、报告问题和提出新需求。

## 报告 Bug

1. 使用 GitHub Issues 的 Bug Report 模板
2. 描述复现步骤：使用的文件格式（xlsx/docx）、期望的输出 vs 实际输出
3. 如可能，附上脱敏后的测试文件

## 提出新功能

1. 使用 GitHub Issues 的 Feature Request 模板
2. 说明使用场景和期望的解决方案
3. 如涉及新设备品类，附 2-3 个该品类典型设备的参数描述样例

## 提 PR

改动请优先在以下文件中进行：

### 解析逻辑 → `scripts/parse_hardware.py`

- 新增或修正分类关键词（HARDWARE_KEYWORDS / SOFTWARE_KEYWORDS / SERVICE_KEYWORDS）
- 优化参数提取正则表达式
- 新增识别规则（如新的强制性标记模式、新的模糊描述模式）

**要求**：
- 保持 JSON 输入输出接口兼容
- 新增关键词需避免误分类（如"虚拟化"可能指软件也可能指功能描述）

### 比对逻辑 → `scripts/compare_quotation.py`

- 优化参数比对逻辑（新增比对维度、修正判定阈值）
- 改进匹配算法（名称匹配、语义匹配）
- 新增偏离判定类型

**要求**：
- 判定阈值需注释说明依据（如"报价 CPU 主频低 ≤5% 但核心数多 ≥20% → 综合判定满足"）
- 保持 JSON 输出格式兼容

### 参考数据 → `references/equipment-db.md`

- 新增经过验证的设备型号和价格
- 修正过期或错误的价格信息

**数据录入要求**：
- 必须标注来源（如"实战-X项目 X厂商报价"或"公开中标价-XX项目"）
- 必须标注日期（年月）
- 仅录入经过验证的价格（🟢），LLM 估算不纳入

### 工作流 → `SKILL.md`

- 新增或优化工作流步骤
- 新增关键规则
- 补充实战验证记录

**要求**：
- 工作流改动不能移除用户确认闸门（Step 1 / Step 2 / 逐厂商比对）
- 新增规则需有实战案例支撑

## 设计决策说明

### 为什么不把"纯询价"和"报价比对"合并成一个脚本？

三个模式的输入不同（无报价 / 有报价 / 有合同），输出结构差异大。Agent 更适合根据当前模式动态调整输出格式，而脚本负责标准化的解析和基础比对工作。

### 为什么用关键词分类而不是 NLP 模型？

硬件/软件/服务的分类关键词相对稳定（新增品类才需新增关键词），关键词匹配在 Agent 环境中透明、可调试、不需要额外依赖。复杂场景下 Agent 可覆盖脚本未匹配的边缘 case。

### 为什么内置库不包含 LLM 估算价格？

LLM 估算价格受模型训练数据时效性影响，且无法标注来源。内置库仅收录经过实战验证或用户确认的价格，保持可信度。Agent 在询价时会同时参考两者并标注置信度。

## 分支策略

- `main` — 稳定版本
- `feature/*` — 新功能开发
- `fix/*` — Bug 修复

PR 请合并到 `main`。
