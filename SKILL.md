---
name: moi-hardware-inquiry
description: 从招标技术文件（docx/xlsx）中提取硬件设备需求，生成询价指引（厂商/型号/参考价格），支持导入厂商报价进行参数比对，自动生成硬件设备参数偏离表（正偏离/负偏离）。当用户需要询价、核价、比对硬件设备参数时使用。
---
# 硬件设备询价与报价比对

## 这个 Skill 做什么

解决投标过程中硬件设备采购的三个核心痛点：

1. **不知道找谁询价** — 从技术规格书/设备清单中提取硬件需求，自动推荐厂商和参考型号
2. **不清楚预算范围** — 给出每项设备的参考价格区间，帮助快速判断报价合理性
3. **人工核对报价太累** — 导入厂商报价单，自动与技术要求逐项比对，生成参数偏离表

## 输入要求

用户需要提供以下之一或组合：

- **技术规格书（docx）**：包含硬件设备技术要求的招标文件
- **设备清单（xlsx）**：结构化的设备名称、规格参数、数量表
- **厂商报价单（xlsx，可选）**：已收到的厂商报价，用于比对

## 关键概念

### 设备分类体系

自动将提取的设备归类，不同类别有不同的询价策略：

| 大类                | 典型设备                            | 询价策略                          |
| ------------------- | ----------------------------------- | --------------------------------- |
| **服务器**    | 应用服务器、数据库服务器、GPU服务器 | 按 CPU/内存/存储/GPU 规格匹配型号 |
| **网络设备**  | 交换机、路由器、防火墙、负载均衡    | 按端口数/吞吐量/功能匹配型号      |
| **存储设备**  | SAN/NAS 存储、备份一体机            | 按容量/IOPS/接口类型匹配型号      |
| **安全设备**  | WAF、IDS/IPS、堡垒机、网闸          | 按吞吐量/功能/合规等级匹配型号    |
| **工控设备**  | PLC、DCS、SCADA、工业防火墙         | 按协议/点数/环境等级匹配型号      |
| **终端设备**  | 工作站、防爆手机、巡检仪、大屏      | 按尺寸/分辨率/防护等级匹配型号    |
| **配件/辅材** | 光模块、线缆、机柜、UPS             | 按规格/数量匹配                   |

### 三种询价信息来源

按优先级依次使用：

1. **内置参考库**（`references/equipment-db.md`）：沉淀的常见设备型号和价格区间，持续更新。**同时作为各类设备核心技术指标的查询基准**——当招标要求模糊或报价参数异常时，用库中同类型设备的典型配置判断合理性
2. **LLM 知识**：模型训练数据中的市场行情（标注"AI 估算"）
3. **用户补充**：用户确认或修正后的信息（标注"用户确认"）

每种来源都有置信度标记：🟢 高（内置库匹配）/ 🟡 中（LLM 估算）/ 🔴 低（需人工确认）

### 参数比对与偏离判定

将厂商报价单中的设备参数与招标要求逐项比对：

| 比对结果               | 说明                                   | 示例                                   |
| ---------------------- | -------------------------------------- | -------------------------------------- |
| ✅**满足**       | 参数等于或优于要求，且在合理范围内     | 要求 64GB 内存，报价 64GB              |
| 🔺**正偏离**     | 参数显著优于要求（可能意味着过度采购） | 要求 64GB 内存，报价 128GB，价格高 40% |
| 🔻**负偏离**     | 参数不满足要求（投标无效风险）         | 要求 64GB 内存，报价 32GB              |
| ⚠️**无法判断** | 报价单中未提供该参数（需向厂商确认）   | 未标注内存规格                         |

---

## 工作流

### Step 1 · 解析硬件需求

**目标**：从技术文件中提取所有硬件设备需求，标准化为结构化清单。

**操作**：

1. 如果输入是 .docx 技术规格书：先用 LLM 逐段扫描，提取所有含硬件参数的段落
2. 如果输入是 .xlsx 设备清单：直接读取表格结构
3. 识别关键字段：设备名称、规格参数、数量、是否强制项（★ / 加粗 / "不低于"）
4. **自动分类**：按关键词将每行分类为「硬件设备」「软件授权」「服务」，分类规则见上文「自动分类」章节

**提取规则**：

- 识别参数关键词：CPU 核数/主频、内存容量、存储容量/类型、网络端口数/速率、电源/功耗、尺寸/重量、防护等级、工作温度等
- 识别强制性标记：★、加粗、标色、"不低于"、"不小于"、"必须"、"强制"
- 模糊描述标记：遇到"主流品牌"、"知名厂商"、"国产优先"等无具体参数的模糊描述，标记为 `【待澄清】`

**输出**：

**硬件设备需求清单**

```markdown
## 📋 硬件设备需求清单

| # | 设备名称 | 关键技术参数 | 数量 | 是否强制 | 澄清状态 |
|:--:|------|------|:--:|:---:|:---:|
| 1 | 数据库服务器 | CPU≥32核/内存≥128GB/存储≥2TB SSD | 2台 | ★强制 | 清晰 |
| 2 | GPU推理服务器 | 需支持AI推理/显存≥24GB | 2台 | 普通 | 【待澄清】显存规格不够具体 |
| 3 | 核心交换机 | 万兆端口≥24个/支持堆叠 | 4台 | ★强制 | 清晰 |
| ... | ... | ... | ... | ... | ... |

**汇总**：共 XX 项，其中强制（★）YY 项，普通 ZZ 项，【待澄清】WW 项，配置自设计 AA 项
```

**软件授权清单**（如有，单独列出）

```markdown
## 💿 软件授权清单（非硬件，不纳入询价比对）

| # | 名称 | 授权要求 | 数量 | 备注 |
|:--:|------|------|:--:|------|
| S1 | Oracle 19C 企业版 | 原厂项目授权+≥1年升级服务 | 1套 | 软件授权 |
| S2 | 超融合虚拟化授权 | 含云管理/计算/网络/存储虚拟化 | 各4套 | 软件授权，子组件合并为一组 |
```

> 📌 软件授权仅在 Step 3 阶段按用户要求参与偏离比对（仅比对授权数量和服务年限），不纳入硬件价格汇总。

⚠️ **必须等用户确认清单后**才能进入 Step 2。用户可补充、修正任意设备的参数。

---

### Step 2 · 生成询价指引

**目标**：为清单中的每项设备推荐厂商、参考型号和价格区间。

> ⚠️ **前置检查**：如果 Step 1 清单中有标记"配置自设计"的设备，必须先完成「方案确认子步骤」（Step 1.5，见上文「配置自设计设备的处理」），用户确认硬件方案后再进入 Step 2。

**操作**：

1. 对每项设备，先查 `references/equipment-db.md` 内置参考库
2. 库中无匹配的，用 LLM 知识补充（标注 🟡）
3. 先在对话中展示 Markdown 格式的询价总览 + 价格汇总 + 待澄清事项
4. 用户确认后，写入 xlsx（询价建议 + 分项询价单多个 Sheet）
5. 告知用户 xlsx 文件路径和各 Sheet 用途

**输出**：Step 2 产出两套文档——**询价建议文档**（给用户自己看）和**分项询价单**（给供应商看，已脱敏）。两套文档写入同一个 xlsx 的不同 Sheet。

---

#### 输出一：📊 询价建议文档（内部用）

写入 Sheet 页：

| Sheet 名               | 内容                                                                       | 用途                                         |
| ---------------------- | -------------------------------------------------------------------------- | -------------------------------------------- |
| **询价总览**     | 全部设备 + 推荐厂商 + 参考型号 + 预估价格区间 + 置信度 + 优先级 + 关键假设 | 用户一眼掌握全局预算和询价策略               |
| **待澄清事项**   | 所有`【待澄清】` 和 `配置自设计` 项，附影响金额和决策建议              | 询价前的阻塞项清单，指导用户先确认什么再询价 |
| **价格汇总**     | 按大类汇总预估总价，硬件与软件授权分开计算                                 | 内部预算编制参考                             |
| **询价策略建议** | 按优先级（🔴立即/🟡本周/🟢可缓）排列 + 建议厂商数量 + 比价要点             | 指导用户分批推进询价                         |

**询价总览 Sheet 格式**：

|  #  | 设备名称      | 推荐询价厂商       | 参考型号            | 预估单价(含税)    | 数量 | 预估小计          | 置信度 | 询价优先级 | 关键假设                           |
| :-: | ------------- | ------------------ | ------------------- | ----------------- | :--: | ----------------- | :----: | :--------: | ---------------------------------- |
|  1  | 数据库服务器  | 浪潮/华为/联想     | NF5280M7 / 2288H V7 | ¥85,000-120,000  |  2  | ¥170,000-240,000 |   🟢   |   🔴立即   |                                    |
|  2  | GPU推理服务器 | 浪潮/新华三/NVIDIA | NF5468M7 / A100-80G | ¥180,000-250,000 |  2  | ¥360,000-500,000 |   🟡   |   🔴立即   | 假设 A100 供货正常，单价 ¥100,000 |
| ... | ...           | ...                | ...                 | ...               | ... | ...               |  ...  |    ...    | ...                                |

**关键假设列**：记录 LLM 估算价格时做的假设（如"假设 RTX 6000 Ada 供货正常，单价 ¥70,000"），用户询价时如假设不成立可快速调整。

---

#### 输出二：📋 分项询价单（外部用，已脱敏）

**脱敏规则**（从 docx 提取的设备参数必须执行；纯 xlsx 清单通常已无敏感信息，酌情处理）：

- ❌ 删除：项目名称、招标编号、甲方名称、分公司具体名称
- ❌ 删除：招标原文的特定表述（如"满足应急管理部XX指南第X条"、"参照XX标准"）
- ✅ 保留：纯技术参数、数量、单位
- ✅ 泛化：甲方名替换为"某能源集团"、地点替换为"区域公司A/B/C/D"

**按供应商类型分 Sheet**，每个 Sheet 可单独导出/复制发给对应供应商：

| Sheet 名                   | 包含设备                          | 可发给                       |
| -------------------------- | --------------------------------- | ---------------------------- |
| **服务器询价单**     | 通用服务器 + AI一体机 + GPU服务器 | 浪潮/超聚变/华为/戴尔/HPE    |
| **视频存储询价单**   | NVR录像机 + 硬盘                  | 华为/海康/大华               |
| **网络与安全询价单** | 交换机/光模块/网闸/防火墙         | 力控/威努特/深信服/山石/锐捷 |
| **终端与外设询价单** | 触控屏/操作电脑/NFC标签/大屏      | MAXHUB/皓丽/联想/恩智浦代理  |

**每个询价单 Sheet 格式**（供应商填写列留空）：

```
供应商名称：[留空]          询价日期：[留空]          有效期至：[留空]

| # | 设备名称 | 规格参数 | 数量 | 单位 | 品牌范围 | 请填写：报价型号 | 请填写：单价(含税) | 请填写：交货周期 | 请填写：质保期 | 备注 |
|:--:|------|------|:--:|:--:|------|:--:|:--:|:--:|:--:|------|
| 1 | AI推理一体机 | CPU≥Xeon 4310/≥128G/≥2×480G SSD/≥16T/国产GPU×4≥24G/≥400路@25fps/双电源 | 1 | 台 | 国产优先 | | | | | 须提供GPU品牌型号 |
| 2 | ... | ... | ... | ... | ... | | | | | |
```

> 📌 "品牌范围"列保留偏好提示（如"国产优先"），但不强制指定品牌，给供应商报价灵活性。如用户有明确品牌倾向可填入。

**询价邮件模板**（对话中输出，不写入 xlsx）：

```
主题：【询价】设备采购询价函 — [项目代号]

致 [供应商名称]：

我司正在为某能源集团安全生产信息化项目进行设备选型，
现就附件中的设备向贵司询价，请于 [日期] 前回复。

询价须知：
1. 请按附件格式填写型号、单价（含13%增值税）、交货周期、质保期
2. 请附产品规格书（Datasheet）
3. 如需澄清技术要求，请联系 [联系人]

谢谢。

[公司名称 / 联系人 / 电话]
```

---

#### 补充输出（用户要求时生成）

**询价跟踪表**（单独 Sheet 或 xlsx）：

| 设备     | 询价厂商 | 发函日期 | 预计回复 | 实际回复 | 报价金额 | 备注 |
| -------- | -------- | -------- | -------- | -------- | -------- | ---- |
| AI一体机 | 超聚变   |          |          |          |          |      |
| AI一体机 | 华为     |          |          |          |          |      |
| ...      | ...      |          |          |          |          |      |

一张表管理所有供应商的询价进度，防止漏跟或漏比价。

---

**询价建议**：对于🟡/🔴置信度的设备，建议用户同时向 2-3 家厂商询价比价。

⚠️ **必须等用户确认**后才能进入 Step 3。用户可调整厂商、型号、价格区间。

---

### Step 3 · 导入报价单并比对

**目标**：将厂商报价单与招标技术要求逐项比对，生成参数偏离表。

**操作**：

1. 用户提供厂商报价单（xlsx），解析其设备清单和参数
2. 将报价单中的设备与 Step 1 的需求清单进行匹配（按设备名称 + 关键参数模糊匹配）
3. 逐项比对参数，判定满足/正偏离/负偏离/无法判断
4. 生成偏离表

**匹配逻辑**：

报价单与需求清单的匹配分两种情况：

**情况一：结构化报价单（与需求同格式）** — 常见于投标人自己按招标清单向供应商询价

- 优先按行号直接对应
- 行号对应不上的，按设备名称 + 关键参数模糊匹配
- 需求清单中的软件授权行自动跳过

**情况二：自由格式报价单（厂商自有格式）** — 常见于不同厂商各自出报价

- 先提取报价单中的所有设备项及其参数
- 按以下优先级依次尝试匹配：
  1. 设备名称精确匹配（如"数据库服务器" ↔ "数据库服务器"）
  2. 设备类别 + 规格范围匹配（如"AI推理一体机 ≥400路" ↔ 报价中400路推理设备）
  3. 功能描述语义匹配（LLM 判断是否为同一设备）
- 每轮匹配后列出结果让用户确认，未匹配上的需求项和报价项分别列出
- 一项需求可能匹配到多个报价（不同厂商），用户可选择保留哪个

**无法自动匹配时的处理**：

- 列出未匹配的需求项："以下需求项未找到对应的报价 → 需补充询价"
- 列出未匹配的报价项："以下报价项未找到对应的需求 → 可能是多余采购或需求遗漏"
- 用户人工指定对应关系后继续比对

**输出**：

```markdown
## 📊 硬件参数偏离表

| # | 设备名称 | 招标要求 | 报价型号 | 报价参数 | 数量 | 单价 | 判定 | 偏离说明 |
|:--:|------|------|------|------|:--:|------|:---:|------|
| 1 | 数据库服务器 | CPU≥32核/内存≥128GB/SSD≥2TB | 浪潮 NF5280M7 | CPU 32核/256GB/SSD 3.84TB | 2 | ¥96,000 | 🔺正偏离 | 内存×2(256GB>128GB)，SSD×1.9(3.84TB>2TB)，价格高12% |
| 2 | GPU推理服务器 | 显存≥24GB | 浪潮 NF5468M7/NVIDIA A100 | 显存80GB | 2 | ¥210,000 | 🔺正偏离 | GPU规格远超要求(A100-80G vs 24GB)，建议确认是否必要 |
| 3 | 核心交换机 | 万兆端口≥24 | H3C LS-6850-56HF | 万兆端口48个 | 4 | ¥42,000 | ✅满足 | |
| 4 | 网闸 | 吞吐量≥500Mbps | 某品牌型号A | 吞吐量200Mbps | 2 | ¥28,000 | 🔻负偏离 | ⚠️ 吞吐量不达标(200<500Mbps)，投标无效风险！ |
| ... | ... | ... | ... | ... | ... | ... | ... | ... |

**汇总**：
| 判定 | 数量 | 说明 |
|:---:|:---:|------|
| ✅ 满足 | XX 项 | 可接受 |
| 🔺 正偏离 | YY 项 | 参数优于要求，需评估是否存在过度采购 |
| 🔻 负偏离 | ZZ 项 | ⚠️ 不满足要求，必须更换或要求厂商重新报价 |
| ⚠️ 无法判断 | WW 项 | 报价单缺失参数，需向厂商确认 |

**负偏离风险提示**：负偏离项可能导致投标无效，建议优先处理。
**正偏离成本提示**：正偏离项合计多出预算约 ¥XXX，如无必要建议调整为满足即可。
```

**输出格式选项**：

- 默认：Markdown 偏离表（对话中直接展示）
- **写入 xlsx**：用户要求时，将偏离判定、汇总统计、风险建议写入原报价文件的三个新 Sheet：
  - `硬件参数偏离比对` — 逐项比对 + 偏离汇总 + 分级风险建议
  - `软件授权偏离比对` — 软件授权逐项比对
  - 原报价 Sheet 新增"偏离判定"列 + 底部汇总
- 写入完成后告知用户文件路径和 Sheet 结构

---

### Step 4 · 多厂商对比与合同比对

Step 3 处理的是单厂商报价 vs 招标要求的逐项比对。实战中经常出现两种更复杂的场景：

#### 场景一：多厂商对比（报价 vs 报价）

当用户提供了 2 家及以上厂商报价时，在 Step 3 单厂商比对完成后，追加多厂商横向对比。

**输出**：独立 xlsx 或写入新 Sheet，包含：

| Sheet | 内容 |
|------|------|
| **逐项同框对比** | 每项设备一行，并列展示各厂商的型号/参数/价格/判定，最后一列给出推荐选择 |
| **价格对比** | 各厂商可比项价格并排，价差%和价格优势方 |
| **综合评估与建议** | 偏离统计对比、关键负偏离汇总、选型推荐、下一步行动 |

**选型推荐逻辑**：
- 双方均达标 → 价格低者胜出
- 一方达标一方不达标 → 达标者胜出，不达标者要求重新选型
- 双方均不达标 → 标记"均需重新报价"

#### 场景二：合同比对（合同 vs 招标）

当用户提供的是拟签采购合同（非报价单）时，启动合同比对模式。

**与报价比对的关键差异**：
- 合同具有法律约束力，参数偏差的后果更严重
- 需要额外检查：缺失项（合同未含但招标要求）、范围外项（合同包含但招标未要求）、质保条款、配件来源
- 输出增加「签约前建议行动」列

**合同比对输出 Sheet**：

| Sheet | 内容 |
|------|------|
| **合同vs招标逐项比对** | 逐项参数对照 + 判定 + 合同价 |
| **缺失与风险分析** | 合同缺失项（附补采费用估算）+ 合同范围外项（防止重复采购）+ 质保/配件风险 |
| **综合评估与建议** | 技术合规/国产化/完整性/价格合理性/质保 五维度评估 + 签约前行动清单 |

---

### 三种比对模式总结

| 模式 | 输入 | 触发条件 | 核心输出 |
|------|------|---------|------|
| **纯询价** | 仅招标需求 | 用户只有技术文件，无任何厂商反馈 | 询价建议文档 + 脱敏分项询价单（Step 2） |
| **报价比对** | 招标需求 + 1-N 家厂商报价 | 用户收到厂商报价 | 单厂商偏离表 + 多厂商对比报告（Step 3） |
| **合同比对** | 招标需求 + 采购合同 | 用户拿到拟签合同 | 合同偏离表 + 缺失项清单 + 签约前建议（Step 4） |

> 📌 三种模式不是严格顺序关系。用户可能跳过询价直接进入报价比对（如已自行询价），也可能跳过报价直接进入合同比对（如单一来源采购）。

---

### MRP 排程衔接（中标后）

本 skill 覆盖**投标前**的询价、核价、合同审核。**中标后**需要排采购计划（每项设备什么时候下单、什么先买什么后买）时，使用独立 skill **`moi-mrp-planning`**。

衔接点：

- 本 skill 的设备清单 JSON（`parse_hardware.py` 输出）可直接作为 `moi-mrp-planning` 的输入（requirements.json）
- 本 skill 分项询价单的**"交货周期"列**（供应商填写）回收后，写入 `moi-mrp-planning` 的 `lead-times.json` 覆盖默认货期
- `moi-mrp-planning` 会检查设备间依赖（服务器依赖网络/机柜先行、软件授权依赖硬件到货），输出最晚下单日期、采购批次与风险清单（MRP 规划.xlsx）

---

## 自动分类：硬件 / 软件 / 服务

招标文件中的设备清单往往混入软件授权和服务类项目。Step 1 解析时必须自动分类，避免把 Oracle 授权、虚拟化许可等当成硬件询价。

### 分类关键词

| 类别               | 关键词                                                                                                                         | 询价策略                                       |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------- |
| **硬件设备** | 服务器、一体机、交换机、路由器、防火墙、网闸、NVR、录像机、工作站、电脑、触控屏、大屏、光模块、机柜、UPS、NFC标签、传感器、PLC | 正常询价流程                                   |
| **软件授权** | 企业版、授权、许可、License、虚拟化（无实体设备）、数据库（Oracle/SQL Server）、中间件、操作系统、容灾授权                     | 单独列表标注"非硬件"，仅比对授权数量和订阅年限 |
| **服务**     | 安装调试、培训、运维、驻场、等保测评、SaaS                                                                                     | 单独列表标注"非硬件"，不纳入硬件询价           |

### 混排清单处理规则

当 xlsx 中软件授权和硬件设备共用一个表格时：

1. 先按关键词逐行分类
2. 软件授权的"组件行"（如深信服超融合的 5 个子组件）合并为一个授权组，不拆分为独立项
3. 输出时分为"硬件设备需求清单"和"软件授权清单"两个表格
4. 硬件报价汇总和软件授权汇总分开计算

---

## 配置自设计设备的处理

部分招标项不指定具体硬件配置，要求投标人自行设计并承诺性能达标。典型场景：

- "支持本地私有化部署 32B 大语言模型，20 用户并发，具体配置由投标人自行设计"
- "支持不低于 20 路 1080P 云渲染，具体配置由投标人自行设计并承诺性能达标"

此类设备在 Step 1 和 Step 2 之间需要增加 **方案确认** 环节：

### 方案确认子步骤（Step 1.5）

1. **识别"配置自设计"标记**：需求中包含"自行设计"、"自行拟定"、"承诺性能达标"等关键词
2. **LLM 给出推荐配置方案**：基于性能指标反推硬件方案（如 32B 模型 → 需 ≥70GB 显存 → 建议 RTX 6000 Ada 96G×2 或 A100 80G×1）
3. **列出方案选项与价格梯度**：
   - 最低可用方案（刚好满足指标，价格最低）
   - 推荐方案（留有余量，性价比最优）
   - 高性能方案（充裕余量，应对未来扩展）
4. **用户确认方案后**，再以确认的方案参数进入 Step 2 正式询价

> ⚠️ 不要跳过方案确认直接询价。32B 大模型服务器的硬件方案差价可达 5-10 倍（最低 ¥150,000 vs 顶配 ¥1,500,000+），未确认方案的价格没有任何参考意义。

---

## 关键规则

1. **清单必须先确认**：Step 1 提取的硬件清单必须等用户确认后才能进入询价
2. **自动分类硬件/软件/服务**：解析时按关键词自动分类，软件授权不纳入硬件询价流程
3. **配置自设计设备先定方案再询价**：未确认硬件方案前不生成价格，给出梯度选项供用户选择
4. **置信度透明**：每项价格建议标注置信度（🟢内置库/🟡LLM估算/🔴需确认）
5. **模糊要求主动标记**：招标要求不清晰时，标记 `【待澄清】`，不强行猜测
6. **负偏离高亮警告**：比对结果中的负偏离项必须醒目标注，这是投标无效的直接风险
7. **正偏离提醒成本**：参数优于要求时，同时标注额外成本，帮助用户判断是否必要
8. **无法判断不等于满足**：报价单中缺失的参数不能默认为满足，必须标记"无法判断"
9. **价格含税标注**：所有价格默认含 13% 增值税，如有特殊情况需标注
10. **询价单必须脱敏**：从 docx 招标文件提取的设备参数，生成外部询价单时必须删除项目名称/甲方名称/招标编号
11. **分厂商类型输出询价单**：同类型设备归入一个 Sheet，按供应商类型拆分，方便用户直接发给对应厂商
12. **内置库持续更新**：每次询价后，用户确认的厂商/型号/价格可沉淀到 `references/equipment-db.md`
13. **内置库双向查询**：`equipment-db.md` 不仅是价格参考，也是各类设备核心技术指标的查询基准。当招标要求模糊（如"配置自设计"设备）或报价参数异常（如防火墙吞吐远超需求）时，用库中同类设备的典型参数作为合理性判断依据
14. **Excel 产物统一样式规范**：所有输出 xlsx（询价总览/分项询价单/偏离表/多厂商对比等）必须美化后再交付，样式标准如下，禁止输出无样式裸表：
   - 表头行：深蓝底 2F5496 + 白字 FFFFFF + 微软雅黑 11 加粗 + 居中 + 细边框（thin）
   - 数据行：微软雅黑 10 + 黑字 + 细边框 + 自动换行 + 垂直居中；序号列/短列居中，长文本列左对齐
   - 分组行/合计行（"小计""合计""总计"等）：浅蓝底 D6E4F0 + 微软雅黑 10 加粗（分组行深蓝字 2F5496，合计行黑字）
   - 列宽：序号列 4-6、短列 8-12、金额列 14-18、长文本列 40-70
   - 行高：按列宽与文本长度估算（全角字符按 2 个宽度单位计），行高 = 所需行数 × 约 14.5，表头行不低于 20，确保全文可见

## 参考文件

- `references/equipment-db.md` — 内置设备参考库（已沉淀网络安全/服务器/存储/网络/软件/终端 30+ 条实战价格）
- `scripts/parse_hardware.py` — 硬件需求解析脚本（支持 xlsx → 结构化清单 + docx → 段落表格提取）✅ 已可用
- `scripts/compare_quotation.py` — 报价比对脚本（需求 JSON + 报价 xlsx → 偏离表 JSON）✅ 已可用

## 实战验证记录

### 三轮实战总览

| 轮次 | 素材 | 类型 | 核心技术挑战 |
|:---:|------|------|------|
| 1 | 素材1-硬件设备 | xlsx 混排清单 | 自动分类（硬件/软件）、配置自设计处理 |
| 2 | 素材3-网络安全 | docx 技术规格书 | docx→结构化提取、双厂商自由格式匹配、多厂商横向对比 |
| 3 | 素材2-云计算 | docx规格书+合同 | 合同比对模式（缺失项检测、范围外项识别、质保条款审查） |

### 2026-06-30 · 完整三轮验证 — 流程与脚本沉淀

- **素材**：1-xlsx混排 + 3-docx纯文本 + 2-docx表格+合同
- **关键发现**：
  1. 软件授权混入硬件清单（素材1）→ 新增「自动分类」章节和关键词表
  2. "配置自设计"设备价格跨度 5-10 倍（素材1）→ 新增「方案确认子步骤（Step 1.5）」
  3. 需求单和报价单同格式 vs 自由格式匹配策略不同（素材1+3）→ Step 3 匹配逻辑区分情况一/情况二
  4. 纯询价 vs 报价比对 vs 合同比对是三种不同模式（三轮实战全部覆盖）→ 新增 Step 4 + 三种比对模式总结
  5. 外部询价单必须脱敏 + 按供应商类型分 Sheet（素材3）→ Step 2 输出重构为输出一+输出二
  6. 合同比对需额外检查缺失项/范围外项/质保条款/配件来源（素材2）→ Step 4 合同比对模式
  7. 脚本沉淀：parse_hardware.py（xlsx+docx→结构化JSON）+ compare_quotation.py（需求×报价→偏离表JSON）
  8. 参考库沉淀：equipment-db.md 已录入 30+ 条经实战验证的设备价格（网络安全/服务器/存储/网络/软件/终端）

